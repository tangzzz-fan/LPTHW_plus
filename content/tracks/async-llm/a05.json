{
  "title": "A05: 流式 chunk（模拟 LLM）",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 5,
  "requires": [],
  "entry": "a05_main.py",
  "body": "# A05: 流式 chunk（模拟 LLM）\n\n来源：本站优先轨 · 企业 LLM 异步落地（原创讲义；手敲练习）。\n\n## 关键概念（先读文档，再敲代码）\n\n| 概念 | 必须讲清 |\n|------|----------|\n| 异步生成器 | `async def` + `yield`：每次 yield 可挂起，适合 token 流 |\n| `async for` | 消费异步可迭代；背后是 `__aiter__` / `__anext__` |\n| 与同步 for | 同步 for 不能直接消费异步生成器 |\n\n**对照 A13**：本课是内存里的流；A13 是 HTTP/SSE 上的流。\n\n## 本题任务\n\n实现 `fake_llm_stream`，用 `async for` 边收边打印（`end=\"\"`）。\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- **关键机制以本题「关键概念」为准**；代码里的撞墙注释只是现场提醒。\n- 少复制粘贴，多手敲；敲错再改。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能用自己的话复述上表中的关键机制\n- [ ] 知道本课在整体轨道里卡住的是哪一环\n",
  "starterFiles": {
    "a05_main.py": "# =============================================================================\n# 自己动手敲（笨方法 / 代码题）：\n# 1) 先读左侧「本题任务 / 关键概念」，再敲下面「已注释」的参考实现（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 关键机制以题目文档为准；代码注释只是现场提示，不要只抄不读讲义。\n# =============================================================================\n\n# A05：异步生成器模拟 LLM token 流。\n# 撞墙记录（预习，敲时对照）：\n#   墙：用普通 for 去迭代 async gen → TypeError。\n\n# import asyncio\n\n\n# async def fake_llm_stream(prompt: str):\n    # tokens = [\"Hello\", \", \", \"this\", \" \", \"is\", \" \", \"streamed\", \" \", \"output\", \".\"]\n    # for t in tokens:\n        # await asyncio.sleep(0.05)\n        # yield t\n\n\n# async def main() -> None:\n    # print(\"prompt accepted\")\n    # async for chunk in fake_llm_stream(\"hi\"):\n        # print(chunk, end=\"\", flush=True)\n    # print(\"\\n[done]\")\n\n\n# if __name__ == \"__main__\":\n    # asyncio.run(main())\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完关键概念与本题任务",
    "手敲参考实现并跑通",
    "能复述关键机制"
  ]
}
