{
  "title": "T4Q4: 记忆分层实验",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 30,
  "requires": [],
  "entry": "q4.py",
  "body": "# T4Q4: 记忆分层实验\n\n来源：inbox `MIT-LLM-Migration` · **RAG与记忆**（一期；原创组装讲义，手敲练习）。\n\n## 主题骨架（摘录）\n\n# T4 教练出题集：RAG 与记忆\n\n\n## 一、共识：5 个核心心智模型\n\n| # | 模型 | 一句话骨架 | iOS 存量锚点 |\n|---|------|-----------|------------|\n| M1 | 语义检索 = 几何近邻 | 文本变向量后，「相关」= 向量距离近；同义改写不改变近邻关系，关键词匹配则直接失联 | 像按内容哈希找资源 vs 按文件名找：一个认内容一个认字面 |\n| M2 | chunking 决定检索上限 | 检索的最小单位是切出来的块：切太大混入噪声、切太小丢上下文，边界处信息必然受损——检索质量问题一半出在切分 | 像分页加载的页大小：粒度是权衡，不是常量 |\n| M3 | RAG 管道五环，错误逐级传播 | 切分 → 向量化 → 检索 → 组装 → 生成：每一环都有独立失败模式，评估要按环定位（召回坏 vs 生成坏是两回事） | 像网络管线：DNS/连接/解码/渲染各环各的错，先定位再修 |\n| M4 | 注入 ≠ 学习 | 检索来的内容只活在本次上下文里：权重不动、会话结束即消失；「给模型喂文档」不是训练 | 像运行时读配置文件 ≠ 改二进制（T1 M2 回收） |\n| M5 | 记忆分层 | 短期记忆 = 上下文窗口（贵、小、会话级）；长期记忆 = 外部存储 + 按需检索（便宜、大、跨会话）；产品「记得你」= 分层的组合拳 | RAM vs 磁盘 + 按需加载：客户端内存管理心智直接平移 |\n\n学员自检要求：合上表复述 M1–M5，并回答「我的 App 想『记住』用户偏好，该用哪一层、为什么」。\n\n## 二、争议：专家吵得最凶的 3 个焦点\n\n**D1 上下文窗口越来越大（百万级），RAG 还有必要吗？**\n- 正方（长上下文派）：全文塞进去，省掉切分/检索两环的错误，模型还能看到全貌。\n- 反方（RAG 派）：成本随长度涨（T2 C6 的 O(n²)）、长文中部信息常被稀释（lost in the middle）、知识更新仍需要外部化；大窗口是互补不是替代。\n- 对迁移者的意义：这是「容量 vs 成本 vs 精度」的三角权衡——给得出边界条件（什么规模下全文塞、什么规模下必须检索）才算懂。\n\n**D2 知识注入用 RAG 还是微调？**\n- RAG 派：知识更新频繁、需要溯源引用、数据敏感不能进权重 → RAG。\n- 微调派：要改的是行为/风格/领域语言习惯而非事实 → 微调；且「教模型事实」用微调本就不可靠。\n- 对迁移者的意义：判据是「变的是知识还是行为」——这与 T3 C6 的决策树是同一把尺子。\n\n**D3 向量检索要不要和关键词检索混用（hybrid）？**\n- 纯向量派：语义统一表示，关键词是过时的补丁。\n- 混合派：专有名词/编号/代码标识符恰恰是 embedding 弱项，BM25 类关键词检索正好补位；加一路 rerank 融合。\n- 对迁移者的意义：知道各自的失败案例（向量搜不到精确编号、关键词搜不到同义改写）比站队重要。\n\n## 三、概念拷问题（6 道，口述通道）\n\n判定标准：能讲出「为什么」并迁移到新场景 = 过；只会搭管道 = 不过。\n\n### C1 为什么关键词搜索不够？语义检索「语义」在哪？\n\n问题：构造一个具体例子：用户查询与答案文档**没有一个字重叠**但仍然相关（同义改写）；解释关键词检索与向量检索在这个例子上的行为差异；再给出一个反例——什么查询是关键词检索反而更靠谱的。\n\n### C2 chunking 切大切小各会出什么问题？\n\n问题：分别给出「块太大」与「块太小」的失败场景（各一个具体例子）；解释重叠（overlap）在缓解什么、代价是什么；最后给出你选块大小的可操作判据（不许只说「看情况」）。\n\n### C3 RAG 五环，哪一环最容易成为瓶颈？怎么定位？\n\n问题：说出五环（切分/向量化/检索/组装/生成）各自的典型失败模式；然后回答：用户抱怨「回答不对」时，你按什么顺序排查、每步看什么证据（要能区分「没检索到」与「检索到了但没用好」）。\n\n### C4 RAG 是训练吗？「喂文档」之后模型发生了什么？\n\n问题：明确回答检索内容去了哪里（权重还是上下文）、会话结束后还在不在、换一个新会话会怎样；再推一步：这决定了 RAG 系统的哪些工程属性（知识更新成本、溯源能力、数据隔离）。\n\n### C5 上下文窗口就是记忆吗？长期记忆应该怎么建？\n\n问题：用 T1 M4（窗口 = 全部记忆）推出「纯上下文记忆」的三个限制；然后设计一个分层记忆方案：什么信息留在上下文、什么存外部、什么时候触发写回与检索。用你的 iOS 内存管理经验做类比并指出断裂处。\n\n### C6 检索质量怎么评估？hit@k 和 MRR 各测什么？\n\n问题：解释两个指标的定义与差异（一个测「在不在前 k」、一个测「排多前」）；设计一个 10 查询的检索评估集骨架：每条要准备什么（查询 + 期望命中的块）、为什么需要「期望不命中」的用例。\n\n## 本题任务\n\n### Q4 记忆分层实验（载体 4.4）\n\n任务：用 T1 Q3 的窗口模型扩展：实现两种对话系统——A「纯上下文」与 B「上下文 + 外挂记忆存储（dict）+ 每轮检索回填」；同一段 12 轮对话（第 1 轮给偏好、第 11 轮问偏好），打印两系统在第 11 轮的表现差异；注释写清：B 的写回/检索触发条件是什么、对应真实系统里的什么组件。\n\n```python\n# 脚手架\nclass MemoryStore:  # 外挂长期记忆\n    def write(self, key, value): ...\n    def retrieve(self, query, k=1): ...\n```\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- 跑通后对照讲义自检；不要先看密卷。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能讲清本题在练哪个模型/机制\n\n## 注释\n- 代码里带有 **【优先课注释】** 与分段说明，先读注释再跑。\n",
  "starterFiles": {
    "q4.py": "# 【优先课注释】MIT-LLM\n# 课题：T4Q4: 记忆分层实验\n# 建议：先通读注释 → 运行看输出 → 改一处参数再跑 → 对照书本加深。\n# ---------------------------------------------------------------------------\n\n# =============================================================================\n# 自己动手敲（笨方法 / MIT 代码题）：\n# 1) 把下面「已注释」的参考实现亲手输入（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 先读讲义任务，再敲代码；不要直接复制粘贴完事。\n# =============================================================================\n\n# T4 Q4 记忆分层实验（参考解法，仅教练分支）\n# 目标：A 纯上下文（带窗口）vs B 上下文+外挂记忆；第 11 轮问偏好见分晓\n\n# def count_tokens(t):\n    # return len(t)\n\n# class MemoryStore:\n    # \"\"\"外挂长期记忆（KV 版）。真实系统里对应：记忆抽取器 + 向量库/DB。\"\"\"\n    # def __init__(self):\n        # self.store = {}\n    # def write(self, key, value):\n        # self.store[key] = value\n    # def retrieve(self, query, k=1):\n        # mock 检索：键包含查询关键词即命中\n        # hits = [(k, v) for k, v in self.store.items() if any(w in k for w in query.split())]\n        # return hits[:k]\n\n# def windowed_reply(history, user_msg, window=120, memory=None):\n    # \"\"\"写回触发条件（mock）：消息含『偏好』关键词 → 存入外挂记忆（对应真实系统的记忆抽取）。\"\"\"\n    # if memory is not None and \"偏好\" in user_msg:\n        # memory.write(\"用户偏好\", user_msg)\n    # msgs = history + [user_msg]\n    # total, i = sum(count_tokens(m) for m in msgs), 0\n    # while total > window and i < len(msgs) - 1:\n        # total -= count_tokens(msgs[i]); i += 1\n    # visible = msgs[i:]\n    # if \"偏好\" in user_msg and \"什么\" in user_msg:\n        # for m in visible:\n            # if \"偏好\" in m and \"什么\" not in m:\n                # return f\"（上下文命中）你的偏好是：{m[:20]}\"\n        # if memory is not None:\n            # hits = memory.retrieve(\"偏好\")\n            # if hits:\n                # return f\"（外挂记忆命中）你的偏好是：{hits[0][1][:20]}\"\n        # return \"我不记得你的偏好了。\"\n    # return f\"收到：{user_msg[:8]}…\"\n\n# if __name__ == \"__main__\":\n    # script = [\"我的偏好是深色模式和紧凑布局，请记住这个偏好。\"] + \\\n             # [f\"第{i}轮闲聊：聊点关于话题{i}的内容和背景{i}。\" for i in range(2, 11)] + \\\n             # [\"我的偏好是什么？\"]\n    # for name, memory in [(\"A 纯上下文\", None), (\"B 上下文+外挂记忆\", MemoryStore())]:\n        # history = []\n        # answer = None\n        # for i, m in enumerate(script, start=1):\n            # reply = windowed_reply(history, m, memory=memory)\n            # history += [m, reply]\n            # if i == len(script):\n                # answer = reply\n        # print(f\"[{name}] 第 {len(script)} 轮回答: {answer}\")\n    # print(\"\\n结论：A 的偏好被挤出窗口后遗忘；B 靠写回（记忆抽取）+检索在新轮次找回。\")\n    # print(\"组件对应：写回触发=记忆抽取器；MemoryStore=向量库/DB；retrieve=按需检索回填上下文。\")\n    # print(\"断裂处：上下文清零是设计不是 bug——LLM 的持久化完全靠外挂（T4 C5）。\")\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完本题任务",
    "手敲参考实现并跑通",
    "能向同事讲清机制"
  ],
  "source": {
    "inbox": "MIT-LLM-Migration",
    "topic": "RAG与记忆",
    "t": 4,
    "q": 4
  }
}
