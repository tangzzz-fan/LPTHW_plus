{
  "title": "T3Q2: fewshot实验台",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 30,
  "requires": [],
  "entry": "q2.py",
  "body": "# T3Q2: fewshot实验台\n\n来源：inbox `MIT-LLM-Migration` · **提示工程与结构化输出**（一期；原创组装讲义，手敲练习）。\n\n## 主题骨架（摘录）\n\n# T3 教练出题集：提示工程与结构化输出\n\n\n## 一、共识：5 个核心心智模型\n\n| # | 模型 | 一句话骨架 | iOS 存量锚点 |\n|---|------|-----------|------------|\n| M1 | 提示 = 上下文内的接口契约 | 模型不记得你的任何约定，全部要求必须写进这一次请求的上下文；提示是「给无状态服务的接口文档」 | 像给无状态 HTTP 服务写接口约定：每次请求都要自包含 |\n| M2 | 消息角色 = 分工协议 | system/user/assistant/tool 各有分工：system 定行为边界、user 给任务、assistant 留历史、tool 回观测；角色是模型训练时认的「槽位」 | 像函数签名里参数的语义标注：类型一样但职责不同 |\n| M3 | few-shot = 上下文学习 | 示例不是装饰，是用「输入→输出样例」直接钉住格式与风格；模型从上下文里的模式续写，不需要改权重 | 像单元测试的 fixture：给样例比写说明书更不容易歧义 |\n| M4 | 结构化输出 = 契约 + 校验 + 重试 | 要求 JSON 只是开始：解析失败/校验失败是常态，schema 校验 + 有限重试 + 降级才是完整管道 | Codable 解码失败怎么办？——同一套纪律：校验、重试、fallback |\n| M5 | 提示评估 = 回归测试 | 提示是代码：改动要有 eval 集回归，两版提示用通过率对比，不接受「感觉更好」 | 改接口不跑单测 = 裸奔；改提示不跑 eval 同理 |\n\n学员自检要求：合上表复述 M1–M5，并各举一个「提示翻车」的场景对号入座。\n\n## 二、争议：专家吵得最凶的 3 个焦点\n\n**D1 提示工程是长期职业还是过渡技能？**\n- 正方：模型接口长期存在「怎么说影响怎么说」，约束/评估/安全边界的工程化只会更重。\n- 反方：模型越强，自然语言直说即可，精巧提示技巧会被模型理解力吞掉；长期看「提示工程师」会并入普通软件工程。\n- 对迁移者的意义：无论站哪边，M4/M5（校验管道与 eval）都是不会过时的部分——争议归争议，工程基建照做。\n\n**D2 system prompt 的权限幻觉与安全边界**\n- 争议：system 被当作「管理员指令」，但它与 user 内容在模型眼里是同源的上下文——提示注入（让数据冒充指令）证明它不是权限边界。\n- 正方立场：system 仍有用（行为默认值、训练期偏置）；反方立场：任何安全关键约束都不该只靠 system，要有外部校验。\n- 对迁移者的意义：把「指令与数据同源」当成安全设计的第一性事实（呼应 T6 权限闸门）。\n\n**D3 few-shot / 提示改写 / 微调 / 换模型，怎么选？**\n- 各派争议在于性价比排序：few-shot 最便宜但吃上下文预算；微调改行为但成本高、更新慢；换大模型简单但贵。\n- 对迁移者的意义：给出「按问题性质选手段」的决策树，而不是默认一种武器。\n\n## 三、概念拷问题（6 道，口述通道）\n\n判定标准：能讲出「为什么」并迁移到新场景 = 过；只能给技巧清单 = 不过。**每题要有机制维度，「我试过好用」不算回答。**\n\n### C1 system 与 user 消息的本质区别是什么？为什么 system 不是「管理员权限」？\n\n问题：说出两者在模型训练期与运行期各自的语义；然后论证：为什么把安全约束只写在 system 里是不充分的？举一个提示注入的最小例子。\n\n### C2 few-shot 为什么有效？示例之间冲突了会怎样？\n\n问题：从 T1 的 next-token 机制解释 few-shot（不许引入「模型学会了」这类说法）；再回答：如果示例格式与文字指令矛盾，模型更可能跟谁、为什么？\n\n### C3 为什么结构化输出必须把「校验与重试」设计进管道？\n\n问题：解释为什么「模型答应输出 JSON」不等于「输出一定是合法 JSON」（从采样机制推）；设计一个校验重试策略：重试几次、重试时给模型回传什么、预算耗尽后降级到什么。\n\n### C4 提示评估和单位测试哪里像、哪里不像？\n\n问题：像的地方给出至少两条（用例/判据/回归）；不像的地方给出至少两条（输出非确定、判据常需语义级）；然后设计一个 10 条用例的评估集骨架：怎么保证用例有区分度（不是全过或全挂）。\n\n### C5 「指令与数据同源」会带来什么攻击面？\n\n问题：解释提示注入的机制（模型无法从结构上区分「你该遵守的规则」与「你要处理的文本」）；给出至少两层防御，并说明各层防的是哪一类注入（直接注入 vs 经外部文档带入的间接注入）。\n\n### C6 一个任务效果差，你按什么顺序排查：改提示 / 加 few-shot / 换模型 / 微调 / 加 RAG？\n\n问题：这是争议题 D3 的落地版。给出你的决策顺序与每一步的判据（什么情况升级手段）；必须说明为什么微调不该是第一选择。\n\n## 本题任务\n\n### Q2 few-shot 实验台（载体 3.2）\n\n任务：扩展 Q1 的 mock：支持「从上下文示例中提取映射规则」（如示例是 `苹果->fruit`、`汽车->vehicle`，则新输入按同类映射回复）。对比 0-shot（无示例，mock 只能回显）与 2-shot 的输出差异，打印对照表，注释解释：这模拟了真实模型的什么机制（上下文模式续写）。\n\n```python\n# 脚手架\ndef mock_llm_fewshot(messages: list[dict]) -> str:\n    \"\"\"从 user/assistant 交替的示例对中提取规则；无示例则回显\"\"\"\n```\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- 跑通后对照讲义自检；不要先看密卷。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能讲清本题在练哪个模型/机制\n",
  "starterFiles": {
    "q2.py": "# 【优先课注释】MIT-LLM\n# 课题：T3Q2: fewshot实验台\n# 建议：先通读注释 → 按注释自己手敲（或去掉行首 #）→ 删占位 print 再跑。\n# ---------------------------------------------------------------------------\n\n# =============================================================================\n# 自己动手敲（笨方法 / MIT 代码题）：\n# 1) 把下面「已注释」的参考实现亲手输入（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 先读讲义任务，再敲代码；不要直接复制粘贴完事。\n# =============================================================================\n\n# T3 Q2 few-shot 实验台（参考解法，仅教练分支）\n# 目标：从上下文示例对提取映射规则；0-shot vs 2-shot 行为差异\n# 机制对照：mock 的「提取规则」模拟真实模型的上下文模式续写（in-context learning 的行为面）\n\n# def mock_llm_fewshot(messages):\n    # users = [(i, m[\"content\"]) for i, m in enumerate(messages) if m[\"role\"] == \"user\"]\n    # 从 user/assistant 交替对中收集示例映射\n    # examples = {}\n    # for i, m in enumerate(messages):\n        # if m[\"role\"] == \"user\" and i + 1 < len(messages) and messages[i + 1][\"role\"] == \"assistant\":\n            # examples[m[\"content\"]] = messages[i + 1][\"content\"]\n    # query = users[-1][1]\n    # if not examples:\n        # return f\"(0-shot 回显) {query}\"\n    # 规则泛化：示例若全是「中文词->英文类别」，对新词按首字同类映射\n    # table = {\"苹果\": \"fruit\", \"香蕉\": \"fruit\", \"汽车\": \"vehicle\", \"飞机\": \"vehicle\"}\n    # if query in table:\n        # return table[query]\n    # return f\"({len(examples)}-shot 仍无法泛化到) {query}\"\n\n# if __name__ == \"__main__\":\n    # zero = [{\"role\": \"user\", \"content\": \"苹果\"}]\n    # two = [\n        # {\"role\": \"user\", \"content\": \"香蕉\"}, {\"role\": \"assistant\", \"content\": \"fruit\"},\n        # {\"role\": \"user\", \"content\": \"汽车\"}, {\"role\": \"assistant\", \"content\": \"vehicle\"},\n        # {\"role\": \"user\", \"content\": \"苹果\"},\n    # ]\n    # print(\"0-shot:\", mock_llm_fewshot(zero))\n    # print(\"2-shot:\", mock_llm_fewshot(two))\n    # print(\"\\n对照表：同一查询，无示例只能回显，有示例则按示例模式输出类别。\")\n    # print(\"机制注记：真实模型没有『提取规则』这一步——是示例序列抬高了\")\n    # print(\"『符合示例格式的输出』的条件概率，模型按上下文模式续写，权重全程不变。\")\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完本题任务",
    "手敲参考实现并跑通",
    "能向同事讲清机制"
  ],
  "source": {
    "inbox": "MIT-LLM-Migration",
    "topic": "提示工程与结构化输出",
    "t": 3,
    "q": 2
  }
}
