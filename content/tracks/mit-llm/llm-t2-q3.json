{
  "title": "T2Q3: 因果mask验证",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 30,
  "requires": [
    "torch"
  ],
  "entry": "q3.py",
  "body": "# T2Q3: 因果mask验证\n\n来源：inbox `MIT-LLM-Migration` · **Transformer与注意力原理**（一期；原创组装讲义，手敲练习）。\n\n## 主题骨架（摘录）\n\n# T2 教练出题集：Transformer 与注意力原理\n\n\n## 一、共识：5 个核心心智模型\n\n| # | 模型 | 一句话骨架 | iOS 存量锚点 |\n|---|------|-----------|------------|\n| M1 | embedding = 语义几何 | 离散 token 查表成向量，语义关系变成向量几何关系（相近 = 相似）；查表本身无智能，智能来自训练出的坐标 | enum 关联一张配置表：只是这次配置的是「语义坐标」 |\n| M2 | 注意力 = 内容寻址的信息路由 | Q 发查询、K 当索引、V 是内容：每个位置按「与谁相关」加权吸收其他位置的信息，权重是 softmax 出来的连续注意力分配 | 像按内容匹配的动态 delegate：不是预先连线，而是每次按「相关度」现场路由 |\n| M3 | 缩放/残差/归一化 = 可训练性的脚手架 | √d 缩放防 softmax 饱和、残差保住信息主干道、LayerNorm 稳住数值——没有它们深网络训不动 | 像 UI 布局的 Auto Layout 约束：不是业务逻辑，但没它整个视图塌掉 |\n| M4 | 位置必须外挂注入 | 注意力本身对打乱顺序无感（置换不变），词序信息要靠位置编码显式注入 | 数组 vs 集合：信息一样，但有没有顺序是另一回事，得显式给 |\n| M5 | 堆叠 = 逐层抽象 | 一层 = 一轮路由；多层把「局部共现」逐层炼成「长程语义依赖」，最后一层投回词表产出 T1 说的 next-token 分布 | 像渲染管线：每 pass 加工一层，最终出图（分布） |\n\n学员自检要求：合上表复述 M1–M5，并把 T1 的 M1（next-token 分布）接上——「分布是哪一步算出来的」要能指出来。\n\n## 二、争议：专家吵得最凶的 3 个焦点\n\n**D1 注意力 O(n²) 太贵，线性注意力/状态空间模型（SSM/Mamba 类）会取代它吗？**\n- 正方：长上下文场景二次成本是真瓶颈；SSM 类推理成本线性且显存友好，多个方向已逼近 Transformer 质量。\n- 反方：注意力的「任意两位置直接交互」表达能力目前仍占优，近似/线性方案在检索类任务上常有隐性退化；生态与硬件优化也全押在 Transformer 上。\n- 对迁移者的意义：工程选型要知道「窗口成本为什么涨」（T1 C4 的机制回收），但追架构新闻不是工程师的必修课——判据是任务上的 eval，不是论文声量。\n\n**D2 Transformer 的成功到底靠注意力还是靠规模？**\n- 一方：消融实验显示去掉注意力（MLP-Mixer 类）在足量数据下也能打，说明规模与数据才是主因。\n- 另一方：同等预算下注意力仍更高效，长程依赖样本效率明显更好。\n- 对迁移者的意义：理解「架构红利 vs 规模红利」的分账，直接影响你对 D1 的判断与对模型选型的预期。\n\n**D3 位置信息怎么给最好（绝对/相对/RoPE）？**\n- 绝对位置编码简单但外推差；相对位置与 RoPE（旋转编码）在长序列外推上更强，已成主流。\n- 争议点：位置该「加在输入」还是「融进注意力计算」，以及外推能力是不是被高估的需求。\n- 对迁移者的意义：你不需要会推导 RoPE，但要能回答「为什么模型知道词序」这个必被追问的问题（M4 的落地）。\n\n## 三、概念拷问题（6 道，口述通道）\n\n判定标准：能讲出「为什么」并迁移到新场景 = 过；只能复述公式 = 不过。**数学存量是入口不是终点：每个矩阵运算都要能说出「这一步在语义上干了什么」。**\n\n### C1 embedding 是什么？为什么「词」能变成向量、还能携带语义？\n\n问题：解释 embedding 查表的过程；回答「向量的坐标是谁定的、凭什么两个意思相近的词向量距离近」；再举一个你会怎么用 embedding 相似度的工程场景（不许只说「搜索」）。\n\n### C2 用一句话讲清 Q/K/V 注意力在做什么？\n\n问题：不许写公式先讲机制：Query/Key/Value 三者的分工、注意力权重怎么产生、加权求和的语义是什么。讲完再写出核心公式，并解释公式里每一项对应你话里的哪个角色。\n\n### C3 注意力为什么要除以 √d？\n\n问题：解释不缩放会发生什么（顺着「点积大小 → softmax 行为 → 梯度」这条链推），以及为什么缩放因子恰好是 √d 而不是 d 或常数。讲不出 softmax 饱和的后果 = 没过。\n\n### C4 模型怎么知道词序？位置信息从哪来？\n\n问题：证明（或解释）注意力本身对顺序无感；然后说明位置编码如何补上这个缺口；再回答：如果两个句子的词完全相同但顺序相反，没有位置编码的模型会把它们当成一样吗？有位置编码呢？\n\n### C5 残差连接与 LayerNorm 各自解决什么问题？\n\n问题：分别解释：① 没有残差时深层网络会发生什么（信息/梯度两个维度）；② LayerNorm 稳住了什么；③ 用一个 iOS 类比说明「主干道 + 支路加工」的结构为什么稳。\n\n### C6 为什么注意力让「长程依赖」比循环结构更容易？代价是什么？\n\n问题：对比 RNN「信息逐站传递」与注意力「任意两位置直连」，解释长程\n\n## 本题任务\n\n### Q3 因果 mask 验证（载体 2.3）\n\n任务：在 Q2 基础上加因果 mask（上三角置 -inf 再 softmax），打印有/无 mask 的权重矩阵对比；构造一个「最后一个位置」的输入扰动实验，证明：无 mask 时第一个位置的输出会被未来的输入影响，有 mask 时不会。用输出差异证明「看不到未来」。\n\n```python\n# 脚手架\ndef causal_mask(n: int) -> np.ndarray: ...\ndef attention_with_mask(X, Wq, Wk, Wv, mask): ...\n# 要求：扰动实验打印前后差异（有 mask 差异为 0，无 mask 非 0）\n```\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- 跑通后对照讲义自检；不要先看密卷。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能讲清本题在练哪个模型/机制\n",
  "starterFiles": {
    "q3.py": "# 【优先课注释】MIT-LLM\n# 课题：T2Q3: 因果mask验证\n# 建议：先通读注释 → 按注释自己手敲（或去掉行首 #）→ 删占位 print 再跑。\n# ---------------------------------------------------------------------------\n\n# =============================================================================\n# 自己动手敲（笨方法 / MIT 代码题）：\n# 1) 把下面「已注释」的参考实现亲手输入（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 先读讲义任务，再敲代码；不要直接复制粘贴完事。\n# =============================================================================\n\n# T2 Q3 因果 mask 验证（参考解法，仅教练分支，numpy）\n# 目标：有/无 mask 权重对比；扰动实验证明「位置 0 看不到未来」\n# import numpy as np\n\n# def softmax(x, axis=-1):\n    # x = x - x.max(axis=axis, keepdims=True)\n    # e = np.exp(x)\n    # return e / e.sum(axis=axis, keepdims=True)\n\n# def causal_mask(n):\n    # 注意：不能用 triu(ones) * -inf（0 * -inf = NaN）——这是本参考实现撞过的墙，留档\n    # future = np.triu(np.ones((n, n)), k=1).astype(bool)\n    # return np.where(future, -np.inf, 0.0)  # 上三角 = 未来位置\n\n# def attention_with_mask(X, Wq, Wk, Wv, mask=None):\n    # d = X.shape[1]\n    # Q, K, V = X @ Wq, X @ Wk, X @ Wv\n    # scores = Q @ K.T / np.sqrt(d)\n    # if mask is not None:\n        # scores = scores + mask\n    # return softmax(scores, axis=1) @ V, softmax(scores, axis=1)\n\n# if __name__ == \"__main__\":\n    # rng = np.random.default_rng(3)\n    # n, d = 4, 8\n    # X = rng.normal(size=(n, d))\n    # Wq = rng.normal(size=(d, d)); Wk = rng.normal(size=(d, d)); Wv = rng.normal(size=(d, d))\n\n    # out_free, w_free = attention_with_mask(X, Wq, Wk, Wv)\n    # out_causal, w_causal = attention_with_mask(X, Wq, Wk, Wv, causal_mask(n))\n    # print(\"无 mask 权重（位置 0 能看全部）:\\n\", np.round(w_free[0], 3))\n    # print(\"因果 mask 权重（位置 0 只能看自己）:\\n\", np.round(w_causal[0], 3))\n\n    # 扰动实验：只改最后一个位置的输入，观察位置 0 的输出\n    # X2 = X.copy(); X2[-1] += 10.0\n    # d_free = np.abs(attention_with_mask(X2, Wq, Wk, Wv)[0][0] - out_free[0]).max()\n    # d_causal = np.abs(attention_with_mask(X2, Wq, Wk, Wv, causal_mask(n))[0][0] - out_causal[0]).max()\n    # print(f\"\\n扰动未来位置后，位置 0 输出的最大变化：\")\n    # print(f\"  无 mask : {d_free:.6f}  <- 被未来影响（非 0）\")\n    # print(f\"  因果mask: {d_causal:.6f}  <- 看不到未来（= 0）\")\n    # assert d_free > 1e-6 and d_causal < 1e-9, \"mask 语义验证失败\"\n    # print(\"断言通过：因果 mask 保证位置 0 对未来扰动完全免疫\")\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完本题任务",
    "手敲参考实现并跑通",
    "能向同事讲清机制"
  ],
  "source": {
    "inbox": "MIT-LLM-Migration",
    "topic": "Transformer与注意力原理",
    "t": 2,
    "q": 3
  }
}
