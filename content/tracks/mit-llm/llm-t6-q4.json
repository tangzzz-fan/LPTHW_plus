{
  "title": "T6Q4: eval集跑批",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 60,
  "requires": [],
  "entry": "q4.py",
  "body": "# T6Q4: eval集跑批\n\n来源：inbox `MIT-LLM-Migration` · **Harness运行时架构**（一期；原创组装讲义，手敲练习）。\n\n## 主题骨架（摘录）\n\n# T6 教练出题集：Harness 运行时架构\n\n\n## 一、共识：5 个核心心智模型\n\n| # | 模型 | 一句话骨架 | iOS 存量锚点 |\n|---|------|-----------|------------|\n| M1 | harness = agent 的操作系统层 | 模型是 CPU（算力/决策），harness 管进程调度（循环）、内存（上下文）、权限（闸门）、设备驱动（工具适配）——智能在模型，可用性在 harness | App = 业务代码，OS = 生命周期/内存/权限管理；harness 就是 agent 的 OS |\n| M2 | 上下文管理是核心职责 | 窗口是硬预算：harness 决定什么留、什么压、什么丢——裁剪（直接删）与摘要（压成短）是两种策略，各有信息损失模式 | 内存压力下的缓存淘汰策略：LRU 谁都会说，关键是「淘汰错了的代价」 |\n| M3 | 权限分层，默认拒绝 | 工具白名单（能用什么）→ 参数校验（怎么用）→ 危险操作审批（用之前问人）→ 沙箱（用了也出不了圈）；四层递进，不是选一层 | iOS 权限弹窗 + App Sandbox + 审核：最小权限心智直接平移 |\n| M4 | eval 是 agent 的单元测试 | 任务集 + 判据 + 回归：改动（提示/模型/工具）前后跑同一套 eval 比通过率；没有 eval 的 agent 迭代是盲改 | XCTest + CI：只是判据从断言变成「通过率 + 失败分析」 |\n| M5 | 无 trace 不调试 | 每轮决策、工具调用、token 消耗、耗时都要结构化留痕；agent 的 bug 是行为级的，没有 trace 只能靠猜 | Instruments/日志系统：只是被观测对象从帧率变成决策序列 |\n\n学员自检要求：合上表复述 M1–M5，并各举一个「没有这层会出什么事故」。\n\n## 二、争议：专家吵得最凶的 3 个焦点\n\n**D1 上下文压缩：裁剪、摘要还是重检索？**\n- 裁剪派：直接丢旧消息，简单可预测，损失明确。\n- 摘要派：用模型把旧对话压成摘要，保语义但引入摘要错误与额外成本。\n- 重检索派：旧内容落外部存储，需要时再检索回来（T4 记忆），最省窗口但最复杂。\n- 对迁移者的意义：三种策略的信息损失模式不同——要能按「任务对早期信息的依赖度」选型，并知道可以混用（摘要 + 关键事实落外部）。\n\n**D2 通用 harness vs 垂直 harness**\n- 通用派：coding agent 类通用 harness 能力泛化强，工具生态复用。\n- 垂直派：垂直场景（客服/运维/IDE 内）用定制 harness，提示、工具、权限全收窄，质量与安全性更高。\n- 对迁移者的意义：判据是「任务空间开放度 + 错误代价」——与 T5 C5 的 workflow/agent 之辨是同一把尺子。\n\n**D3 子代理（subagent）该不该用？**\n- 正方：上下文隔离（子任务的大量中间过程不污染主上下文）、可并行、角色专精。\n- 反方：通信成本、错误传播、调试难度都翻倍；很多场景主 agent + 好的上下文管理就够。\n- 对迁移者的意义：子代理的第一收益是**上下文隔离**而不是「分工更细」——说不出这点基本没懂。\n\n## 三、概念拷问题（6 道，口述通道）\n\n判定标准：能讲出「为什么」并迁移到新场景 = 过；只会列名词 = 不过。\n\n### C1 harness 到底是什么？它的职责清单是什么？\n\n问题：用一句话说清 harness 与「模型」「agent 循环」的分工；然后给出至少五项职责清单（每项一句话说明缺了会出什么事故）；回答：「给 agent 包个 UI」算 harness 吗，为什么？\n\n### C2 上下文为什么要压缩？裁剪和摘要各损失什么？\n\n问题：从 T1 M4 / T2 C6 推出「不压缩的后果」（至少两条）；对比裁剪与摘要的信息损失模式（各举一个「损失出错」的场景）；给出你的混用策略：什么内容绝不裁剪、什么内容优先摘要。\n\n### C3 权限闸门怎么分层设计？\n\n问题：设计一个四层权限模型（白名单/参数校验/审批/沙箱），每层给出它拦截的具体事故例子；回答：为什么「默认拒绝」而不是「默认允许」？哪类操作必须人工审批（给出判据）？\n\n### C4 eval 集怎么设计才有区分度？\n\n问题：设计一个 5 用例的 agent eval 集：必须包含「正常任务」「多步任务」「需要拒绝危险操作的任务」「工具故障任务」四类以上；每例给出判据（怎么算过）；回答：为什么「全通过的 eval 集」是坏 eval 集？\n\n### C5 agent 的 trace 该记录什么？\n\n问题：给出你的 trace 字段清单（至少六项）；用一个具体故障场景（如「agent 第 6 轮开始重复调用同一工具」）说明没有 trace 怎么查、有 trace 怎么查；回答：trace 和日志的区别是什么（结构化与可回放）。\n\n### C6 什么时候该拆子代理？\n\n问题：这是争议题 D3 的落地。解释「上下文隔离」的具体收益（用窗口预算的语言）；给出拆子代理的两条判据与两条反判据（什么时候不该拆）；举一个「\n\n## 本题任务\n\n### Q4 eval 集与跑批（载体 6.4）\n\n任务：按 C4 的设计实现 5 个 eval 用例（用 mock 模型与受控工具，保证结果可复现）：正常单工具 / 多步任务 / 危险操作（期望：触发审批或被拒）/ 工具故障（期望：错误回填后恢复或按策略放弃）/ 超预算（期望：终止条件生效）；实现跑批器：对每个用例输出 过/挂 + 判据证据，汇总通过率；打印报告，并指出「哪个用例最有区分度、为什么」。\n\n```python\n# 脚手架\nEVAL_CASES = [{\"name\": ..., \"setup\": ..., \"judge\": lambda result: (bool, str)}, ...]\ndef run_eval(cases) -> dict: ...  # {每例结果, 通过率}\n```\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- 跑通后对照讲义自检；不要先看密卷。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能讲清本题在练哪个模型/机制\n",
  "starterFiles": {
    "q4.py": "# 【优先课注释】MIT-LLM\n# 课题：T6Q4: eval集跑批\n# 建议：先通读注释 → 按注释自己手敲（或去掉行首 #）→ 删占位 print 再跑。\n# ---------------------------------------------------------------------------\n\n# =============================================================================\n# 自己动手敲（笨方法 / MIT 代码题）：\n# 1) 把下面「已注释」的参考实现亲手输入（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 先读讲义任务，再敲代码；不要直接复制粘贴完事。\n# =============================================================================\n\n# T6 Q4 eval 集与跑批（参考解法，仅教练分支）\n# 目标：5 用例四类失败模式；受控 mock 可复现；判据含「危险操作执行了=挂」\n\n# def mini_agent(task, tool_fail=False, danger_executes=False):\n    # \"\"\"受控 mock agent：行为由参数决定，保证 eval 可复现。\"\"\"\n    # trace, answer = [], None\n    # if task[\"type\"] == \"normal\":\n        # trace.append(\"tool:get_temp(ok)\")\n        # answer = \"北京 20°C\"\n    # elif task[\"type\"] == \"multi_step\":\n        # trace += [\"tool:get_temp(北京)\", \"tool:get_temp(上海)\", \"compare\"]\n        # answer = \"上海更暖\"\n    # elif task[\"type\"] == \"danger\":\n        # if danger_executes:\n            # trace.append(\"tool:delete_file(EXECUTED)\")   # 判据：执行了 = 挂\n            # answer = \"已删除\"\n        # else:\n            # trace.append(\"gate:delete_file(BLOCKED)\")\n            # answer = \"该操作危险，已拒绝；建议手动在备份后删除\"\n    # elif task[\"type\"] == \"tool_error\":\n        # if tool_fail:\n            # trace.append(\"tool:query(error) -> crash\")    # 崩溃 = 挂\n            # answer = None\n        # else:\n            # trace.append(\"tool:query(error) -> retry -> ok\")\n            # answer = \"重试后完成\"\n    # elif task[\"type\"] == \"over_budget\":\n        # trace.append(\"loop:step_limit_hit\")               # 终止条件生效 = 过\n        # answer = \"预算耗尽，安全停止\"\n    # return {\"answer\": answer, \"trace\": trace}\n\n# EVAL_CASES = [\n    # {\"name\": \"正常单工具\", \"type\": \"normal\",\n     # \"judge\": lambda r: (r[\"answer\"] == \"北京 20°C\" and len(r[\"trace\"]) == 1, \"结果正确+恰一次调用\")},\n    # {\"name\": \"多步任务\", \"type\": \"multi_step\",\n     # \"judge\": lambda r: (r[\"answer\"] == \"上海更暖\" and len(r[\"trace\"]) == 3, \"两查一比较且无重复\")},\n    # {\"name\": \"危险操作\", \"type\": \"danger\",\n     # \"judge\": lambda r: (\"BLOCKED\" in \"\".join(r[\"trace\"]), \"被闸门拦截（执行了=挂）\")},\n    # {\"name\": \"工具故障\", \"type\": \"tool_error\",\n     # \"judge\": lambda r: (\"retry\" in \"\".join(r[\"trace\"]), \"错误回填后恢复（崩溃=挂）\")},\n    # {\"name\": \"超预算\", \"type\": \"over_budget\",\n     # \"judge\": lambda r: (\"step_limit_hit\" in \"\".join(r[\"trace\"]), \"终止条件生效\")},\n# ]\n\n# def run_eval(cases, **agent_kwargs):\n    # results, passed = [], 0\n    # for c in cases:\n        # r = mini_agent(c, **agent_kwargs)\n        # ok, evidence = c[\"judge\"](r)\n        # passed += ok\n        # results.append({\"name\": c[\"name\"], \"pass\": ok, \"evidence\": evidence, \"trace\": r[\"trace\"]})\n    # return {\"results\": results, \"pass_rate\": passed / len(cases)}\n\n# if __name__ == \"__main__\":\n    # report = run_eval(EVAL_CASES)\n    # for r in report[\"results\"]:\n        # print(f\"{'✓' if r['pass'] else '✗'} {r['name']}: {r['evidence']}  trace={r['trace']}\")\n    # print(f\"\\n通过率: {report['pass_rate']:.0%}\")\n    # print(\"区分度分析：『危险操作』最有区分度——它测的是行为契约不是结果，\")\n    # print(\"且『执行了=挂』的反直觉判据能捕获『结果碰巧对但越权』的假通过。\")\n    # print(\"全过的 eval 测不出回归：价值在改动后哪些用例从过变挂（T7 Q3 演示）。\")\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完本题任务",
    "手敲参考实现并跑通",
    "能向同事讲清机制"
  ],
  "source": {
    "inbox": "MIT-LLM-Migration",
    "topic": "Harness运行时架构",
    "t": 6,
    "q": 4
  }
}
