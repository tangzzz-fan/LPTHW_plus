{
  "title": "T5Q2: ReAct循环",
  "priority": true,
  "outlineOnly": false,
  "timeoutSec": 60,
  "requires": [],
  "entry": "q2.py",
  "body": "# T5Q2: ReAct循环\n\n来源：inbox `MIT-LLM-Migration` · **Agent循环与工具调用**（一期；原创组装讲义，手敲练习）。\n\n## 主题骨架（摘录）\n\n# T5 教练出题集：Agent 循环与工具调用\n\n\n## 一、共识：5 个核心心智模型\n\n| # | 模型 | 一句话骨架 | iOS 存量锚点 |\n|---|------|-----------|------------|\n| M1 | agent = 模型决策 + 工具执行 + 循环 | 三要素缺一即不是 agent：模型决定「下一步做什么」（不是代码写死），工具把决定变成对世界的动作，循环把结果喂回去直到完成 | 事件循环 + delegate：只是「事件处理器」换成了模型 |\n| M2 | function calling = 类型化的调用契约 | 工具以 JSON schema 声明（名字/参数/描述），模型输出结构化调用请求，宿主解析执行——模型从不直接执行，只「提议」 | 像 protocol 方法签名：模型只能发出符合签名的调用，宿主才是实现方 |\n| M3 | 观察回填 = 循环的燃料 | 工具结果（含报错）作为 tool 消息进上下文，成为下一轮决策的输入；没有回填就没有循环，只有单次调用 | 像异步回调：结果回来才能走下一步，错误也是结果 |\n| M4 | 终止条件是一等公民 | 成功信号（模型宣布完成）+ 硬上限（最大轮数/token 预算）+ 停滞检测（重复同一动作）——三者缺一，循环就可能烧钱空转 | 像重试预算 + 超时：网络层没人敢写无限重试 |\n| M5 | workflow 与 agent 的分界 = 谁在决策 | 路径由代码写死 = workflow（可控、可预测）；路径由模型运行时选择 = agent（灵活、难预测）；多数业务先 workflow，不确定时才上 agent | 像硬编码路由 vs 动态调度：能用 switch 解决就别上运行时反射 |\n\n学员自检要求：合上表复述 M1–M5，并用 M5 判一个你见过的「AI 自动化产品」到底是 workflow 还是 agent。\n\n## 二、争议：专家吵得最凶的 3 个焦点\n\n**D1 多 agent 协作是进步还是过度设计？**\n- 正方：角色分工（规划者/执行者/评审者）带来上下文隔离与专精，复杂任务可分解。\n- 反方：agent 间通信本身引入新的错误传播与成本叠加，很多「多 agent」只是把提示拆成了几段；单 agent + 好工具常常更强。\n- 对迁移者的意义：判据是「上下文隔离与分工是否真的降低了单 agent 的负担」——为架构而架构在 agent 领域同样成立（呼应设计模式线的反滥用纪律）。\n\n**D2 ReAct（边想边做）vs Plan-and-Execute（先规划后执行）**\n- ReAct 派：每步都拿最新观测决策，适应变化；Plan 派：先出全局计划再执行，省调用、易审计。\n- 实际共识在中间：计划可以随观测修订（replan），纯任何一种都有失败模式。\n- 对迁移者的意义：给出「任务可预见性」作为选型维度，而不是背流派。\n\n**D3 该给模型多大自由度？**\n- 宽松派：限制越多 agent 越废，靠模型自身判断 + 事后审计。\n- 约束派：工具白名单、参数校验、危险操作审批是刚需，自由度是设计出来的不是放出来的。\n- 对迁移者的意义：这是 T6 权限闸门前哨——你的 iOS「最小权限」直觉在这里直接可用。\n\n## 三、概念拷问题（6 道，口述通道）\n\n判定标准：能讲出「为什么」并迁移到新场景 = 过；只会调框架 API = 不过。\n\n### C1 agent 的最小结构是什么？它和「自动化脚本」的本质区别在哪？\n\n问题：画出（或口述）agent 循环的最小闭环，指出三个不可省略的组件；然后论证：一个写死步骤的「调 LLM 三次」的脚本为什么不是 agent？「模型决策」具体改变了什么（路径空间、失败模式、测试方式各说一条）。\n\n### C2 function calling 为什么需要 schema？没有 schema 会怎样？\n\n问题：解释工具 schema（名字/参数类型/描述）对模型的三重作用；推演：如果只给模型一段自然语言工具说明、让它自由输出调用文本，会多出哪两类故障（解析侧与行为侧）？schema 里的「描述」字段为什么不是可有可无的？\n\n### C3 工具执行失败了，agent 该怎么办？\n\n问题：对比三种策略：直接崩溃 / 静默重试 / 把错误作为观察回填让模型决策；论证为什么第三种是 agent 的正确姿势（从 M3 推）；再给出回填的边界：什么样的错误不该让模型处理（要熔断/上报的）。\n\n### C4 循环什么时候停？只有「最大轮数」够吗？\n\n问题：给出至少三种终止机制并说明各防什么失控；构造一个「轮数没到但已经空转」的场景（重复同一工具调用），说明停滞检测怎么设计；token 预算为什么也应该进终止条件。\n\n### C5 workflow 和 agent 怎么选？\n\n问题：这是争议题 D1/D2 的落地。给出你的判据（至少两条，如任务路径的可预见性、错误的代价）；举一个「应该从 agent 退回 workflow」的例子和一个「workflow 撑不住必须上 agent」的例子。\n\n### C6 多轮工具调用的「状态」住在哪里？\n\n问题：agent 跑了 8 轮，第 9\n\n## 本题任务\n\n### Q2 ReAct 循环最小实现（载体 5.2）\n\n任务：在 Q1 基础上实现循环：任务 → 模型决策（调工具 or 给最终答案）→ 执行 → 观察回填 → 再决策……直到模型输出最终答案或达到最大轮数。mock 模型规则自定（如「先查天气，再查温度换算，最后总结」三步任务），打印每轮的思考/行动/观察三段；断言：循环恰好在预期轮数终止。\n\n```python\n# 脚手架\ndef react_loop(task: str, max_steps=5) -> dict:\n    \"\"\"返回 {\"answer\": ..., \"trace\": [每轮记录], \"steps\": n}\"\"\"\n```\n\n## 动手要求\n\n- 起始代码已用注释标出：**请自行输入**（或去掉 `#`），再删除占位 `print`。\n- 跑通后对照讲义自检；不要先看密卷。\n\n## 自检\n\n- [ ] 去掉注释后能运行\n- [ ] 能讲清本题在练哪个模型/机制\n\n## 注释\n- 代码里带有 **【优先课注释】** 与分段说明，先读注释再跑。\n",
  "starterFiles": {
    "q2.py": "# 【优先课注释】MIT-LLM\n# 课题：T5Q2: ReAct循环\n# 建议：先通读注释 → 运行看输出 → 改一处参数再跑 → 对照书本加深。\n# ---------------------------------------------------------------------------\n\n# =============================================================================\n# 自己动手敲（笨方法 / MIT 代码题）：\n# 1) 把下面「已注释」的参考实现亲手输入（或去掉行首 #）\n# 2) 删掉最底部的占位 print\n# 3) 保存并运行（⌘/Ctrl+Enter）\n# 先读讲义任务，再敲代码；不要直接复制粘贴完事。\n# =============================================================================\n\n# T5 Q2 ReAct 循环最小实现（参考解法，仅教练分支）\n# 目标：思考/行动/观察三段式 trace；双终止条件；断言轮数\n\n# def mock_decision(task, observations):\n    # \"\"\"mock 模型决策：按任务关键词 + 已见观察推进（重点在循环骨架不在 mock 智能）。\"\"\"\n    # temps = {o[\"city\"]: o[\"temp\"] for o in observations if \"temp\" in o}\n    # if \"北京\" in task and \"北京\" not in temps:\n        # return {\"thought\": \"需要先查北京气温\", \"action\": {\"tool\": \"get_temp\", \"city\": \"北京\"}}\n    # if \"上海\" in task and \"上海\" not in temps:\n        # return {\"thought\": \"还需要上海气温\", \"action\": {\"tool\": \"get_temp\", \"city\": \"上海\"}}\n    # if len(temps) >= 2:\n        # diff = temps[\"北京\"] - temps[\"上海\"]\n        # return {\"thought\": \"两地都查到了，可以比较\", \"final\": f\"北京比上海{'高' if diff>0 else '低'}{abs(diff)}°C\"}\n    # return {\"thought\": \"信息不足\", \"final\": \"无法完成\"}\n\n# def mock_tool(action):\n    # return {\"city\": action[\"city\"], \"temp\": {\"北京\": 20, \"上海\": 26}[action[\"city\"]]}\n\n# def react_loop(task, max_steps=5):\n    # observations, trace = [], []\n    # for step in range(1, max_steps + 1):\n        # decision = mock_decision(task, observations)\n        # trace.append({\"step\": step, \"thought\": decision[\"thought\"],\n                      # \"action\": decision.get(\"action\") or \"最终答案\"})\n        # if \"final\" in decision:\n            # return {\"answer\": decision[\"final\"], \"trace\": trace, \"steps\": step}\n        # obs = mock_tool(decision[\"action\"])\n        # observations.append(obs)               # 观察回填 = 循环的燃料（M3）\n        # trace[-1][\"observation\"] = obs\n    # return {\"answer\": \"达到最大轮数，未完成\", \"trace\": trace, \"steps\": max_steps}\n\n# if __name__ == \"__main__\":\n    # result = react_loop(\"比较北京和上海的气温\")\n    # for t in result[\"trace\"]:\n        # print(t)\n    # print(f\"\\n最终答案: {result['answer']}（共 {result['steps']} 步）\")\n    # assert result[\"steps\"] == 3, \"预期：查北京→查上海→比较，恰好 3 步\"\n    # print(\"断言通过：循环在最终答案处终止；max_steps 是另一条保险丝（M4）。\")\n\n# =============================================================================\n# 写完练习后，删除下面这行占位再运行：\nprint(\"(请在上方自行输入代码)\")\n"
  },
  "checklist": [
    "读完本题任务",
    "手敲参考实现并跑通",
    "能向同事讲清机制"
  ],
  "source": {
    "inbox": "MIT-LLM-Migration",
    "topic": "Agent循环与工具调用",
    "t": 5,
    "q": 2
  }
}
