# 01 · Local scaffold: Vite React + FastAPI, UI hits /api/health

Status: done
Type: AFK

## What to build

Monorepo boots with one command (or documented two). Frontend and backend run locally; a simple UI page can call `GET /api/health` and show a healthy response.

## Acceptance criteria

- [x] `apps/web` (Vite React TS) and `apps/server` (FastAPI) exist
- [x] Root script or README can start both
- [x] CORS allows local Vite origin
- [x] UI displays health payload from the API

## Blocked by

None - can start immediately
